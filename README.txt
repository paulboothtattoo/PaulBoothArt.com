Replace BOTH root files in PaulBoothArt.com:
- index.html
- styles.css

This version uses a full-button absolute centering wrapper, so the label and arrow are forced into the exact vertical and horizontal center regardless of inherited line-height or older header styles.
